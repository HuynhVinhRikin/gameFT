<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Jumper - written with Phaser 2</title>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/phaser/2.0.7/phaser.min.js"></script>
    <link rel="stylesheet" href="./game.css" />
  </head>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
  <script type="text/javascript" src='https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.1.3/socket.io.js'></script>
  <body><script type="text/javascript" src="./game.js"></script>
    TRÒ chuyện tại đây
  <div id='chatBox' style = 'width: 300; height: 200px; background-color:antiquewhite'>
    <a>user</a><p>tin nhắn</p><br><br>
  </div>
  <input id='messageText' type = 'text' placeholder="soạn tin">
   <button id='sendMessage'>Gửi</button>
  </body>

  <script>
    $(document).ready(function(){
      $('#sendMessage').click(function(){
        let value = $('#messageText').val();
        socket.emit('CSS_SEND_MESSAGE', { name : `user${userID}`,mesage : value});
      })
      
      socket && socket.on('SSC_SEND_MESSAGE', resp =>{
        
        $('#chatBox').append(`
          <a>${resp.name}</a><p>${resp.mesage}</p><br><br>
        `)
      })
    })
    hello()
    // this.socket = io();

    // createUser()
  </script>
</html>
